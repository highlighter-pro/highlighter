export type backgroundStateType = {
    currentHighlightId: string | undefined,
};

const backgroundState: backgroundStateType = {
    currentHighlightId: undefined
};

export default backgroundState;